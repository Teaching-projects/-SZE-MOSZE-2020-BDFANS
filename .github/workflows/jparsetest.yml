name: jparser test

on:
    push
    
jobs:
    jparsetest:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: sudo ./gtestinit.sh
            - run: ./jparsetest.sh --gtest_list_tests
            - uses: actions/upload-artifact@v2
              with:
                name: JparseTest Artifact
                path: jparsetest_output.txt